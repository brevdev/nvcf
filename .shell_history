export HISTFILE=.shell_history
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-new-prompts-to-generate.txt"' |tee list-of-new-prompts-to-generate.txt
export HISTFILE=.shell_history
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-new-prompts-to-generate.txt"' |tee list-of-new-prompts-to-generate.txt
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-new-prompts-to-generate.txt"' |tee list-of-new-prompts-to-generate.txt
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-new-prompting-strategies.txt"' |tee list-of-new-prompting-strategies.txt
clear
history ctx-exec
history |grep ctx-exec
git st
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-improvement-toolchain-scripts-to-generate.sh"' |tee list-of-improvement-toolchain-scripts-to-generate.txt
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-extra-prompting-tricks" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad,' |tee list-of-extra-prompting-tricks
history |grep ctx-exec
git st
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-improvement-toolchain-scripts-to-generate.sh"' |tee list-of-improvement-toolchain-scripts-to-generate.txt
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-extra-prompting-tricks" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad,' |tee list-of-extra-prompting-tricks
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefil options' |tee list-of-extra-prompting-tricks
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefil options' |tee list-of-extra-prompting-tricks
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefil options' |tee list-of-extra-prompting-tricks
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
grep prefill *
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
grep prefill *
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
grep prefill *
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
ccat list-of-extra-prompting-tricks2
cat list-of-extra-prompting-tricks2
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
ls
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
ccat list-of-extra-prompting-tricks2
cat list-of-extra-prompting-tricks2
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
ls
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
(echo 'output a good meta-prompt for claude to extend this codebase'; ~/code-to-gpt.sh; ctx-exec cgpt -h>&1; ctx-exec cgpt --show-advanced-usage all) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option' |tee list-of-extra-prompting-tricks2
(echo 'output a bash creation script that uses AI and techniques in .shell_history'; ~/code-to-gpt.sh; ctx-exec cgpt -h>&1; ctx-exec cgpt --show-advanced-usage all) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="list-of-files-to-generate" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option..
(echo 'output a bash creation script that uses AI and techniques in .shell_history'; ~/code-to-gpt.sh; ctx-exec cgpt -h>&1; ctx-exec cgpt --show-advanced-usage all) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="./generate-bash-script" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option..
(echo 'output a bash creation script that uses AI and techniques in .shell_history'; ~/code-to-gpt.sh; ctx-exec cgpt -h>&1; ctx-exec cgpt --show-advanced-usage all) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="./generate-bash-script" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option..
(echo 'output a bash creation script that uses AI and techniques in .shell_history'; ~/code-to-gpt.sh; ctx-exec cgpt -h 2>&1; ctx-exec cgpt --show-advanced-usage all) |cgpt -s 'you are an expert claude meta prompter. use <antthinking> and <scratchpad> tags heavily' -p '<ant-meta-thought eventual-goal="./generate-bash-script" hint="use shell history, ant-thinking, ant-meta-thought, scratchpad, and leverage the cgpt cli tool with the system prompting and prefill option.'
pbpaste > generate-task.sh
chmod +x generate-task.sh
git st .
bash generate-task.sh 'add function create subcommand'
rg nvcf-go
go doc github.com/tmc/nvcf-go
go doc github.com/tmc/nvcf-go > nvcf-go-sdk-api.txt
bash generate-task.sh 'add function create subcommand'
watch -n 5 -cd 'go install && nvcf fn list'
watch -n 5 -cd 'go install && nvcf fn list'
bash generate-task.sh 'add function create subcommand'
watch -n 5 -cd 'go install && nvcf fn list'
rm add_function_create_subcommand.go 
watch -n 5 -cd 'go install && nvcf fn list'
nvcf fn
nvcf fn create
clear
nvcf fn create
clear
bash generate-task.sh 'add function create subcommand'
bash generate-task.sh 'add function create version subcommand'
bash generate-task.sh 'add function versions list'
bash generate-task.sh 'add function versions list'
go install
mv add_function_create_version_subcommand.go add_function_create_version_subcommand.go.x
go install
mv add_function_versions_list.go add_function_versions_list.go.x
go install
nvcf 
nvcf functions
nvcf functions list
ngc 
nvcf functions list
nvcf functions create
pbpaste > deploy-function-version.sh
rg pexec
nvcf fn list --visibility=private
cd nvcf
ls
cat list-of-*
ls
cat generate-task.sh 
./generate-task.sh 'add functionn delete subcommand'
export CGPT_MODEL=claude-3-opus-2024022
./generate-task.sh 'add functionn delete subcommand'
cgpt -s 'hi'
echo hi | cgpt --debug
git st .
git co -b meta
git add list-of-* nvcf-go-sdk-api.txt *sh cmd/* .shell_history 
